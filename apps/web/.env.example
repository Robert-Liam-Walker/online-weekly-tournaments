VITE_API_URL=http://localhost:3001
# Optional — Socket.io now shares the API origin. Only set if the socket host
# differs from the page origin (dev: leave unset, vite proxies /socket.io).
#VITE_SOCKET_URL=
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
